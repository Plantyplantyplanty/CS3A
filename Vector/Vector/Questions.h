#pragma once
//When declaring equality, do you care if the capacities are equal?

//When declaring default constructor outside class, it states that default arguments are not 
//allowed.

//Do you still want the debug "Reallocating" statements from Add_entry?
//More about error handling: throw an exception? Just have error? what does pop_back() return if it's empty?